import 'package:flutter/material.dart';

class WidgetDoBoleto extends StatelessWidget {
  const WidgetDoBoleto({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('boleto');
  }
}
